<%= form_for(@membership) do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :person_id, value: @person.user_id %>
  </div>
  <div class="field">
  <!--  We pass the organization code in here instead of the organization_id. It gets parsed in the controller. -->
    <%= f.label :organization_id, "Organization Code:" %><br>
    <%= f.text_field :organization_id, value: nil %>
  </div>
  <div class="actions">
    <%= f.submit "Connect", class: "btn btn-success" %>
  </div>
<% end %>
